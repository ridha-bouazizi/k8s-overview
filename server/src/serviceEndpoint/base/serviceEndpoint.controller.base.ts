/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { ServiceEndpointService } from "../serviceEndpoint.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ServiceEndpointCreateInput } from "./ServiceEndpointCreateInput";
import { ServiceEndpointWhereInput } from "./ServiceEndpointWhereInput";
import { ServiceEndpointWhereUniqueInput } from "./ServiceEndpointWhereUniqueInput";
import { ServiceEndpointFindManyArgs } from "./ServiceEndpointFindManyArgs";
import { ServiceEndpointUpdateInput } from "./ServiceEndpointUpdateInput";
import { ServiceEndpoint } from "./ServiceEndpoint";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ServiceEndpointControllerBase {
  constructor(
    protected readonly service: ServiceEndpointService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "ServiceEndpoint",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ServiceEndpoint })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: ServiceEndpointCreateInput
  ): Promise<ServiceEndpoint> {
    return await this.service.create({
      data: data,
      select: {
        icon: true,
        id: true,
        name: true,
        updatedAt: true,
        url: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "ServiceEndpoint",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [ServiceEndpoint] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(ServiceEndpointFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<ServiceEndpoint[]> {
    const args = plainToClass(ServiceEndpointFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        icon: true,
        id: true,
        name: true,
        updatedAt: true,
        url: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "ServiceEndpoint",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ServiceEndpoint })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: ServiceEndpointWhereUniqueInput
  ): Promise<ServiceEndpoint | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        icon: true,
        id: true,
        name: true,
        updatedAt: true,
        url: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "ServiceEndpoint",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ServiceEndpoint })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: ServiceEndpointWhereUniqueInput,
    @common.Body() data: ServiceEndpointUpdateInput
  ): Promise<ServiceEndpoint | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          icon: true,
          id: true,
          name: true,
          updatedAt: true,
          url: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "ServiceEndpoint",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ServiceEndpoint })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: ServiceEndpointWhereUniqueInput
  ): Promise<ServiceEndpoint | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          icon: true,
          id: true,
          name: true,
          updatedAt: true,
          url: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
